import React from 'react';

interface GalleryActionsProps {
  galleryId: string;
  galleryName: string;
  isOwner: boolean;
}

export function GalleryActions({ galleryId, galleryName, isOwner }: GalleryActionsProps) {
  // Componente rimosso - nessuna azione disponibile al momento
  return null;
}