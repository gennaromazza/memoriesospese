rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ===== Helper Functions =====
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isAdmin() {
      return isSignedIn() && (
        request.auth.token.email == 'gennaro.mazzacane@gmail.com' ||
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin'
      );
    }

    function isValidRole(role) {
      return role == 'bride' || role == 'groom';
    }

    // ✅ Controlla che il token sia valido e non scaduto
    function hasValidToken(galleryId, role, tokenId) {
      let tokenDoc = get(/databases/$(database)/documents/questionnaireTokens/$(tokenId));
      return tokenDoc.exists()
        && tokenDoc.data.galleryId == galleryId
        && tokenDoc.data.role == role
        && request.time < tokenDoc.data.expiresAt
        && !(tokenDoc.data.revoked == true);
    }

    // ===== Users =====
    match /users/{userId} {
      allow read: if isSignedIn();
      allow write: if isOwner(userId) || isAdmin();
    }

    // ===== Subscriptions =====
    match /subscriptions/{userId} {
      allow read: if isOwner(userId) || isAdmin();
      allow write: if false; // Solo via webhook
    }

    // ===== Galleries =====
    match /galleries/{galleryId} {
      allow read: if true;
      allow create: if isSignedIn();
      allow update: if isSignedIn() &&
        (resource.data.userId == request.auth.uid || isAdmin());
      allow delete: if isSignedIn() &&
        (resource.data.userId == request.auth.uid || isAdmin());

      // ----- Subcollections -----
      match /photos/{photoId} {
        allow read: if true;
        allow create: if isSignedIn();
        allow update, delete: if isSignedIn() &&
          (resource.data.uploaderEmail == request.auth.token.email ||
           resource.data.uploaderUid == request.auth.uid || isAdmin());
      }

      match /comments/{commentId} {
        allow read: if true;
        allow create: if isSignedIn();
        allow update, delete: if isSignedIn() &&
          (resource.data.userId == request.auth.uid || isAdmin());
      }

      match /voiceMemos/{memoId} {
        allow read: if true;
        allow create: if true;
        allow update, delete: if isSignedIn() &&
          (resource.data.userId == request.auth.uid || isAdmin());
      }
    }

    // ===== Global Photos =====
    match /photos/{photoId} {
      allow read: if true;
      allow create: if isSignedIn();
      allow update: if isSignedIn() &&
        (resource.data.uploaderEmail == request.auth.token.email || isAdmin());
      allow delete: if isSignedIn() &&
        (resource.data.uploaderEmail == request.auth.token.email || isAdmin());
    }

    // ===== Comments =====
    match /comments/{commentId} {
      allow read: if true;
      allow create: if isSignedIn();
      allow update: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow delete: if isSignedIn() &&
        (resource.data.userId == request.auth.uid || isAdmin());
    }

    // ===== Voice Memos =====
    match /voiceMemos/{memoId} {
      allow read: if true;
      allow create: if true;
      allow update: if isSignedIn() && resource.data.userId == request.auth.uid;
      allow delete: if isSignedIn() &&
        (resource.data.userId == request.auth.uid || isAdmin());
    }

    // ===== Likes =====
    match /likes/{likeId} {
      allow read: if true;
      allow create: if isSignedIn();
      allow delete: if isSignedIn() && resource.data.userId == request.auth.uid;
    }

    // ===== Email Notifications =====
    match /email-notifications/{notificationId} {
      allow read: if isAdmin();
      allow create: if isSignedIn() &&
        request.resource.data.keys().hasAll(['email', 'galleryId']) &&
        request.resource.data.email is string &&
        request.resource.data.galleryId is string;
      allow update: if isAdmin();
      allow delete: if isAdmin();
    }

    // ===== Gallery Access =====
    match /gallery-access/{accessId} {
      allow read: if isAdmin();
      allow create: if true;
      allow update: if isAdmin();
      allow delete: if isAdmin();
    }

    // ===== Studio Settings =====
    match /studio-settings/{settingId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /settings/{documentId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ===== FAQ =====
    match /faqSets/{faqSetId} {
      allow read: if true;
      allow write: if isAdmin();
      allow create: if isAdmin() ||
        (request.resource.data.createdBy == 'system' &&
         request.resource.data.version == 1);
    }

    // ===== Questionnaire Tokens =====
    match /questionnaireTokens/{tokenId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // ===== Validation Sessions =====
    match /validationSessions/{sessionId} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if isAdmin();
    }

    // ===== Rate Limits =====
    match /rateLimits/{identifier} {
      allow read: if true;
      allow create, update: if true;
      allow delete: if isAdmin();
    }

    // ===== Questionnaires =====
    match /galleries/{galleryId}/questionnaires/{questionnaireId} {
      allow read: if true;

      // Risposte definitive
      match /answers/{role} {
        allow read, write: if
          isValidRole(role) &&
          request.query.token != null &&
          hasValidToken(galleryId, role, request.query.token);
      }

      // Bozze risposte
      match /drafts/{role} {
        allow read, write: if
          isValidRole(role) &&
          request.query.token != null &&
          hasValidToken(galleryId, role, request.query.token);
      }
    }
  }
}
