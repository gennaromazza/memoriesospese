# 👇 PROMPT PER REPLIT (Bolt AI)
Analizza il progetto Firebase Functions attualmente presente nella cartella `functions/`
e segui questi step:

1️⃣ **Verifica la versione di Firebase Functions in uso**
   - Controlla in `functions/package.json` se viene importato `firebase-functions/v2` oppure `firebase-functions`.
   - Se trovi `firebase-functions/v2`, significa che il progetto usa **Cloud Functions v2**.
   - Se invece trovi solo `firebase-functions`, è **v1** (classico).

2️⃣ **Crea un breve riepilogo nel log:**
   - Mostra versione trovata
   - Mostra se c’è già gestione CORS
   - Indica se viene usato Express oppure `onRequest` diretto

3️⃣ **Applica automaticamente il fix CORS appropriato:**

   - ✅ Se è **v2**:
     Trova l’export della function `getGalleryMetadata` (o crea se non esiste) e aggiungi:
     ```ts
     export const getGalleryMetadata = onRequest({
       cors: [
         "http://localhost:5173",
         "http://localhost:3000",
         "https://65b2695f-b200-424b-9509-5c251e0658fe-00-8qf1iviax1yg.spock.replit.dev",
         "https://gennaromazzacane.it"
       ],
       region: "us-central1"
     }, async (req, res) => {
       // tua logica esistente...
     });
     ```

   - ✅ Se è **v1**:
     Inserisci **in cima al file** (dopo gli import) questo frammento:
     ```ts
     const allowed = new Set([
       "http://localhost:5173",
       "http://localhost:3000",
       "https://65b2695f-b200-424b-9509-5c251e0658fe-00-8qf1iviax1yg.spock.replit.dev",
       "https://gennaromazzacane.it"
     ]);
     ```
     E poi aggiungi all’interno della tua `https.onRequest`:
     ```ts
     const origin = req.headers.origin;
     const allowOrigin = allowed.has(origin) ? origin : "*";
     if (req.method === "OPTIONS") {
       res.set("Access-Control-Allow-Origin", allowOrigin);
       res.set("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
       res.set("Access-Control-Allow-Headers", "Content-Type, Authorization");
       res.status(204).send("");
       return;
     }
     res.set("Access-Control-Allow-Origin", allowOrigin);
     ```
     Inseriscilo **prima** della logica della risposta principale.

4️⃣ **Non modificare altre funzioni** oltre `getGalleryMetadata`.

5️⃣ Alla fine stampa un riepilogo finale con:
   - Versione rilevata (v1 o v2)
   - Linee modificate
   - Conferma se il CORS ora gestisce `OPTIONS` e `Access-Control-Allow-Origin`.

⚠️ Non rimuovere nulla dalle altre funzioni; aggiungi solo il necessario.
