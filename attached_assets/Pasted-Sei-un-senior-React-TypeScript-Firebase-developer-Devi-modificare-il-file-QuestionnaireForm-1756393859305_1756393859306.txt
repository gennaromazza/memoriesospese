Sei un senior React + TypeScript + Firebase developer.

Devi modificare il file `QuestionnaireForm.tsx` del progetto Memorie Sospese.

🎯 Obiettivo: Risolvere il bug per cui, dopo aver aggiornato la pagina, il token del questionario viene marcato come "scaduto" anche se non lo è.

---

### **Requisiti**
1. **Persistenza completa del token**
   - Se `localStorage.getItem('questionnaire-token')` esiste e corrisponde al token dell'URL, usa quello.
   - Se non esiste, salva il token corrente una volta validato.
2. **Persistenza stato validazione**
   - Aggiungi `localStorage.setItem('questionnaire-token-valid', 'true')` quando il token è stato validato correttamente.
   - Aggiungi `localStorage.setItem('questionnaire-session', validation.sessionId)` se presente.
3. **Salta la validazione remota se già validato**
   - Se `localStorage.getItem('questionnaire-token-valid') === 'true'` e `localStorage.getItem('questionnaire-session')` esiste, **non richiamare** `TokenValidationService.validateTokenAndCreateSession`.
4. **Aggiornamento URL**
   - Dopo la prima validazione, rimuovi `?token=...` dai parametri per evitare conflitti.

---

### **Implementazione dettagliata**
- **Step 1:** All'inizio del componente, sostituisci:
```ts
const tokenFromUrl = urlParams.get('token');
const [token, setToken] = useState<string | null>(() => {
  return tokenFromUrl || localStorage.getItem('questionnaire-token');
});
con:

ts
Copia codice
const tokenFromUrl = urlParams.get('token');
const [token, setToken] = useState<string | null>(() => {
  return tokenFromUrl || localStorage.getItem('questionnaire-token');
});
const [hasValidated, setHasValidated] = useState(
  localStorage.getItem('questionnaire-token-valid') === 'true'
);
Step 2: Aggiorna la funzione validateTokenAndLoadData():

Se hasValidated === true e localStorage.getItem('questionnaire-session') esiste → salta validazione remota e chiama direttamente loadQuestionnaireData(questionnaireId) recuperando questionnaireId da localStorage.

Altrimenti, procedi come ora con la validazione remota e, se valida, salva:

ts
Copia codice
localStorage.setItem('questionnaire-token', token);
localStorage.setItem('questionnaire-token-valid', 'true');
localStorage.setItem('questionnaire-session', validation.sessionId);
localStorage.setItem('questionnaire-id', validation.questionnaireId);
Step 3: Non modificare nulla nella logica di autosave, caricamento bozze o invio finale.

Risultato atteso
La prima volta che apro il link → il token viene validato e salvato ✅

Ricarico la pagina → non chiamo di nuovo la validazione → uso direttamente il token salvato ✅

Se ricevo un nuovo link con un altro token → questo sovrascrive il vecchio e viene validato ✅

Nessun errore di "token scaduto" al refresh ✅