RUOLO:
Sei uno sviluppatore senior Node.js/Firebase Functions. Devi AGGIORNARE il file gmail.ts (o gmail.js se non uso TS) in modo che l'invio email funzioni sia dalla funzione sendGalleryPasswordV2 che dalla funzione sendNewPhotosNotificationPublic, senza più causare "Cannot read properties of undefined (reading 'settings')". NON fare domande, applica e riscrivi le funzioni necessarie.

OBIETTIVO:
- Uniformare gestione credenziali Gmail.
- Accettare SIA singolo destinatario che array di destinatari.
- Non usare più proprietà tipo `.settings` su valori potenzialmente undefined.
- Loggare errori in modo chiaro.

CONTESTO:
Ho una variabile segreta REPL_IDENTITY collegata via runWith({ secrets: ['REPL_IDENTITY'] }) nelle Cloud Functions.
Questa REPL_IDENTITY contiene un token/JSON di autenticazione che ci permette di inviare email via Gmail API (OAuth service account o simile).
Nelle funzioni:
- sendGalleryPasswordV2 (https.onRequest) funziona e invia email.
- sendNewPhotosNotificationPublic (https.onRequest) chiama sendGmailEmail(...) e attualmente crasha in getAccessToken su ".settings".

Quindi devi:
1. Riscrivere getAccessToken e getGmailClient in gmail.ts in modo che leggano le credenziali SOLO da process.env.REPL_IDENTITY (o process.env['REPL_IDENTITY']) e NON da oggetti tipo config.replit.settings ecc.
2. Se REPL_IDENTITY è una stringa JSON, fai JSON.parse. Se è già oggetto, usalo direttamente. Se è undefined, fai throw con errore esplicito "Missing REPL_IDENTITY".
3. Gestire refresh token / access token: se nel progetto attuale già stai creando OAuth2Client con client_id, client_secret, refresh_token, o se stai usando un service account JWT, mantieni la stessa logica che già usi nelle email funzionanti. Se nel codice esistente c'è già una parte funzionante usata da sendGalleryPasswordV2, RIUTILIZZA QUEL CODICE COME SORGENTE DI VERITÀ. Quello è il ramo buono. Rimuovi/ignora eventuali rami alternativi che fanno controlli tipo identity.settings ecc. Vogliamo UNA sola strada, quella che funziona per sendGalleryPasswordV2.

4. Riscrivere sendGmailEmail(destinatari, subject, htmlBody):
   - `destinatari` può essere string oppure array di string.
   - Normalizza così:
     ```js
     const toList = Array.isArray(destinatari) ? destinatari : [destinatari];
     ```
   - Costruisci il messaggio Gmail con quei destinatari come "to" join(", ").
   - Usa sempre lo stesso sender mittente che già stai usando nell'implementazione esistente funzionante (quello che usi per l'invio password). NON cambiare il from.
   - In caso di errore fai `functions.logger.error('❌ Gmail send error', err);` e rilancia l'errore.

5. Esporta anche le funzioni HTML builder esistenti (createNewPhotosEmailHTML, createGalleryPasswordEmailHTML, ecc.) SENZA modifice tranne il fatto che possono restare tali e quali. NON devi toccare il contenuto HTML.

6. Aggiungi log utili:
   - Prima di inviare l'email:
     ```js
     functions.logger.info(
       \`📧 Sending email to \${toList.length} recipient(s): \${toList.join(', ')} | subject="\${subject}"\`
     );
     ```

7. Il file finale gmail.ts/gmail.js deve esportare almeno:
   - sendGmailEmail
   - createNewPhotosEmailHTML
   - createGalleryPasswordEmailHTML
   - createWelcomeEmailHTML (se esiste già)
   - createTestEmailHTML (se esiste già)

8. IMPORTANTISSIMO:
   La funzione sendGmailEmail NON deve più tentare di accedere a `identity.settings` o simili. Nessun `.settings`. Nessun accesso tipo config.gmail.settings.
   Se mancano le credenziali in REPL_IDENTITY, fai:
   ```js
   throw new Error('Missing REPL_IDENTITY or invalid Gmail credentials');
9- Dopo aver riscritto gmail.ts/gmail.js, assicurati che sendGalleryPasswordV2 e sendNewPhotosNotificationPublic possano semplicemente fare:
const { sendGmailEmail, createNewPhotosEmailHTML } = await import('./gmail');
await sendGmailEmail(recipients, subject, htmlContent);

e funzioni per entrambi i casi (singolo destinatario string o lista di email).

10- NON cambiare i nomi delle funzioni esistenti nel resto del progetto. Mantieni gli stessi export in modo che il resto del codice non rompa.

APPLICA ORA LA PATCH AL FILE gmail.ts/gmail.js.